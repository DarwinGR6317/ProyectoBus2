<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoBus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/yellow2</item>
        <item name="colorPrimaryVariant">@color/yellow2</item>
        <item name="colorOnPrimary">@color/yellow2</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>
    <style name="Theme.ProyectoBus" parent="Base.Theme.ProyectoBus"/>

    <style name="Theme.App.ProyectoBus" parent="Theme.ProyectoBus">
        <item name="windowSplashScreenBackground">@color/yellow</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/autobus</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ProyectoBus</item>
    </style>

</resources>